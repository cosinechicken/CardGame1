<Page
    x:Class="IndependentProject.Play"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:IndependentProject"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:data="using:IndependentProject"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <Grid>
        <StackPanel>
            <ScrollViewer HorizontalScrollMode="Enabled" HorizontalScrollBarVisibility="Auto">
                <GridView ItemsSource="{x:Bind Cards}" HorizontalAlignment="Center" IsItemClickEnabled="True" ItemClick="GridView_Click">
                    <GridView.ItemTemplate>
                        <DataTemplate x:DataType="data:Card">
                            <StackPanel>
                                <TextBlock Text="{x:Bind Set}"/>
                                <TextBlock Text="{x:Bind SetLast}"/>
                            </StackPanel>
                        </DataTemplate>
                    </GridView.ItemTemplate>
                </GridView>
            </ScrollViewer>
            <TextBlock Name="TestBlock" Text="yeyyy" FontSize="16"/>
        </StackPanel>
    </Grid>
</Page>
